
from setuptools import setup, find_packages

setup(
    name='my_channel_viewer',
    version='0.0.1',
    description="Test app to view channels, tune them and make a figure from them",
    packages=find_packages(),
    keywords=['omero', 'example'],
)
